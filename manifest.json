{/* 画面切り替え用 */
.screen { display: none; height: 100vh; }
.screen.active { display: flex; flex-direction: column; }

.menu { width: 100%; background: white; border: none; }
.menu:active { opacity: 0.7; }

.topbar{
  display:flex;
  align-items:center;
  gap:10px;
  padding:12px 14px;
  border-bottom:1px solid #ccc;
}
.topbar h1{ font-size:18px; margin:0; }
.back{
  border:none;
  background:white;
  font-size:18px;
}
.content{ padding:14px; }

  "name": "部署アプリ",
  "short_name": "部署",
  "start_url": ".",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#f7f2e8",
  "icons": []
}

